.main__message-list--block{data: {message: {id: message.id}}}
  .header
    .header__name
      = message.user.name
    .header__date
      = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
  .body
    = message.messages
    = image_tag message.image.url, class: 'Message__image' if message.image.present?